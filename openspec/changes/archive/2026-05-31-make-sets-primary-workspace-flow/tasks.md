## 1. Planning

- [x] 1.1 Add OpenSpec proposal, design, tasks, and spec deltas.
- [x] 1.2 Validate the OpenSpec change.

## 2. Workspace Model

- [x] 2.1 Add a pure helper to create a balanced-element set from an existing set.
- [x] 2.2 Add tests proving the new set balances Fire/Water/Earth/Air and leaves the source set unchanged.

## 3. Navigation UI

- [x] 3.1 Reframe build and set pages with user-facing "set" copy.
- [x] 3.2 Add a build-page action to create a balanced set variant.
- [x] 3.3 Keep builder and optimizer actions scoped to the selected set.

## 4. Verification

- [x] 4.1 Run focused research workspace tests.
- [x] 4.2 Run the full test suite.
- [x] 4.3 Run the production build.
- [x] 4.4 Run OpenSpec strict validation for `make-sets-primary-workspace-flow`.
- [x] 4.5 Verify the set-first flow in the browser and capture the current state.
